<% if(validation.length > 0){ %>
  
<div class="alert alert-danger">
  <strong>Ops!</strong> Falta pouco para efetuar seu cadastro. Por favor,
  corrija
  <% if(validation.length > 1) { %>
  os seguintes erros
  <%} else {%>
  o seguinte erro
  <% } %>
</div>
<ul class="list-group">
  <% for(let i = 0; i < validation.length; i++){%>
  <li class="list-group-item list-group-item-danger">
    <%= validation[i].msg %>
  </li>
</ul>
  <% }
  %><br /><%
} %>